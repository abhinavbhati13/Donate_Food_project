<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.food_donate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FOOD_DONATE">
        <activity android:name=".AvailableDelivery"></activity>
        <activity android:name=".SearchFood" />
        <activity android:name=".TrackFood" />
        <activity android:name=".DeliverFood" />
        <activity android:name=".NGOPage" />
        <activity android:name=".AddFood" />
        <activity
            android:name=".DonateFood"
            android:label="@string/title_activity_donate_food"
            android:theme="@style/Theme.FOOD_DONATE.NoActionBar" />
        <activity
            android:name=".Registration"
            android:label="@string/title_activity_registration"
            android:theme="@style/Theme.FOOD_DONATE.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>